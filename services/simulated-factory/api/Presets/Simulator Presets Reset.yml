info:
  name: Simulator Presets Reset
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:{{port}}/api/presets/reset
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
