info:
  name: Simulator Presets Run
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:{{port}}/api/presets/run
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
