info:
  name: Simulator Presets List
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:{{port}}/api/presets
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
