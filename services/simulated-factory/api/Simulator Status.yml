info:
  name: Simulator Status
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:{{port}}/api/status
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
