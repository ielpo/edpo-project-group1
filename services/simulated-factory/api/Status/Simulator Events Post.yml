info:
  name: Simulator Events Post
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:{{port}}/api/events
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
