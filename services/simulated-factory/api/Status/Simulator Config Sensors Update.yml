info:
  name: Simulator Config Sensors Update
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:{{port}}/api/config/sensors/{{sensorId}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
