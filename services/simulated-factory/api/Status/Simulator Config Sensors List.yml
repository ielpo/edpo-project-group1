info:
  name: Simulator Config Sensors List
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:{{port}}/api/config/sensors
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
