info:
  name: Simulator Health
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:{{port}}/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
