info:
  name: Dobot Color
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:{{port}}/api/dobot/{{name}}/color
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
