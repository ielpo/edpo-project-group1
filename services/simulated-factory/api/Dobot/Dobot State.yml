info:
  name: Dobot State
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:{{port}}/api/dobot/{{name}}/state
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
