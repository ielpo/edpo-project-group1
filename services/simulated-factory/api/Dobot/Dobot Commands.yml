info:
  name: Dobot Commands
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:{{port}}/api/dobot/{{name}}/commands
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
