info:
  name: Dobot IR
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:{{port}}/api/dobot/{{name}}/ir
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
