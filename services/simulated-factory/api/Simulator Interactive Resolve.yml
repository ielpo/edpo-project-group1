info:
  name: Simulator Interactive Resolve
  type: http
  seq: 19

http:
  method: POST
  url: http://localhost:{{port}}/api/interactive/{{actionId}}/resolve
  auth: inherit
  body:
    mimeType: application/json
    text: |
      {
        "outcome": "success",
        "reason": "operator approved"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
