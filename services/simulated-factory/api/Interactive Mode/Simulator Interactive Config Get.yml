info:
  name: Simulator Interactive Config Get
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:{{port}}/api/interactive/config
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
