info:
  name: Simulator Interactive Pending
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:{{port}}/api/interactive/pending
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
