info:
  name: Simulator Interactive Config Put
  type: http
  seq: 6

http:
  method: PUT
  url: http://localhost:{{port}}/api/interactive/config
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
