[project]
name = "dobot-control"
version = "1"
requires-python = ">=3.13"
description = "Application to control the Dobot Magician robots via USB"
readme = "README.md"

dependencies = [
   "flask",
   "pydantic",
   "pydobotplus",
   "pyyaml",
   "werkzeug",
]
